// @pymes/db — schema Prisma, migraciones y seeds (docs/plan/03).
// Fase 0: stub. El schema.prisma inicial y la migracion base con RLS
// se agregan como tarea separada de la fase 0.

export const DB_PACKAGE_READY = false;
