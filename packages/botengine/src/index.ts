// @pymes/botengine — orquestacion de Claude y definicion de tools (docs/plan/01 §3.1).
// Fase 0: stub. El motor real (tools por permisos, presupuesto de tokens)
// se implementa en fase 2 con el modulo del bot.

export const BOTENGINE_PACKAGE_READY = false;
